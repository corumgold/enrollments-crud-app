const initState = {
  data: [
    {
      firstName: "Cory",
      lastName: "Gold",
      email: "goldcorum@gmail.com",
      gpa: 4.0,
    },
  ],
};

const studentReducer = (state = initState, action) => {
  return state;
};

export default studentReducer;
